# Быстрый старт

## 1. Установка зависимостей
```bash
pip install -r requirements.txt
```

## 2. Настройка config.py

1. Скопируйте `config.example.py` в `config.py` (или отредактируйте существующий `config.py`)

2. Получите API данные:
   
   📖 **Подробная инструкция:** Смотрите файл `API_SETUP.md` для пошагового руководства
   
   Кратко:
   - Откройте https://my.telegram.org/apps
   - Войдите с вашим номером телефона (введите номер в формате +79991234567)
   - Введите код подтверждения, который придет в Telegram
   - После входа вы увидите страницу "App configuration"
   - Если у вас еще нет приложения, нажмите "Create new application"
     * **App title**: только английские буквы (например: "My Reposter" или "Telegram Bot")
     * **Short name**: только английские буквы, цифры и подчеркивание, БЕЗ пробелов (например: "reposter" или "mybot")
     * Остальные поля можно оставить пустыми
     * Нажмите "Create"
     * ⚠️ Если ошибка "Incorrect app name!" - используйте только английские буквы!
   - На странице вы увидите:
     * **App api_id** - это число (например: 12345678) - скопируйте его
     * **App api_hash** - это строка из букв и цифр (например: abcdef1234567890...) - скопируйте её

3. Заполните `config.py`:
   ```python
   API_ID = 12345678  # Ваш API ID (число!)
   API_HASH = 'ваш_api_hash'  # Ваш API Hash
   SOURCE_CHANNEL = '@чужой_канал'  # Канал для мониторинга
   TARGET_CHANNEL = '@ваш_канал'  # Ваш канал
   YOUR_CHANNEL_LINK = 'https://t.me/ваш_канал'  # Ссылка на ваш канал
   CHECK_INTERVAL = 10  # Проверка каждые 10 секунд
   ```

## 3. Запуск
```bash
python telegram_reposter.py
```

При первом запуске введите:
- Номер телефона (с кодом страны, например: +79991234567)
- Код подтверждения из Telegram
- Пароль двухфакторной аутентификации (если включен)

## 4. Проверка работы

После запуска вы увидите:
```
🚀 Бот запущен!
📡 Мониторинг канала: Название чужого канала
📤 Публикация в канал: Название вашего канала
👂 Ожидание новых постов...
```

Когда в чужом канале появится новый пост, он автоматически будет скопирован в ваш канал с заменой ссылок.

## Важно!

- ✅ Убедитесь, что вы подписаны на исходный канал
- ✅ Убедитесь, что у вас есть права администратора в целевом канале
- ✅ Для минимальной задержки используйте `CHECK_INTERVAL = 10`
- ✅ Бот работает в реальном времени через события Telegram API

## Остановка

Нажмите `Ctrl+C` для остановки бота.


